syntax = "proto3";
package stockbuddy;
option go_package = "stockbuddy/protos/quote_go_proto";

message Quote {
	string symbol = 1;
	int32 timestamp = 2;
	float open = 3;
	float high = 4;
	float low = 5;
	float close = 6;
	float adj_close = 7;
	int32 volume = 8;
}

message QuoteRequest {
	string symbol = 1;
	int32 period = 2;
}

message QuoteResponse {
	repeated Quote quotes = 1;
}

service QuoteService {
	rpc ListQuoteHistory(QuoteRequest) returns (QuoteResponse);
}
